<br>
<div class="p-6 mx-auto bg-white rounded-lg shadow-lg max-w-screen-xl">

  <div class="flex justify-between">
    <div class="w-3/4 px-2 py-4 border border-gray-100 rounded-lg shadow-lg mb-7">
      <% @tags.each do |tag| %>
        <span class="px-2 py-1 text-white bg-gray-600 rounded-lg hover:bg-slate-500"><%= link_to tag.name, keyboards_path(tag_id: tag.id) %></span>
      <% end %>
    </div>
      <%= link_to '楽天市場に移動する', @keyboard.url, method: :get, class: "border border-gray-100 rounded-lg shadow-lg mb-7 bg-red-500 font-bold hover:bg-red-600 text-white text-2xl hover:text-white text-center py-2 px-4 hover:border-transparent rounded" %>
  </div>

  <div class="text-center text-gray-700">
    <h2 class="mb-10 text-2xl font-bold"><%= @keyboard.name %></h2>
    <div class="flex justify-center mb-7 image-gallery gap-4">
      <% @keyboard.medium_image_urls.each do |image| %>
        <%= image_tag image, class: "w-64 h-64 object-cover" %>
      <% end %>
    </div>
    <div class="w-full px-2 py-4 border border-gray-100 rounded-lg shadow-lg mb-7">
      <h3 class="mb-2 text-4xl font-bold"><span class="text-red-500">価格 </span><%= @keyboard.price %>円</h3>
    </div>
    <div class="w-full px-2 py-4 rounded-lg shadow-lg mb-7">
      <h3 class="mb-2 text-2xl font-bold text-red-500">対応のOS</h3>
      <h3 class="mb-2 text-2xl font-bold">
        <% @keyboard.os.each do |os| %>
          <%= os %>
        <% end %>
      </h3>
    </div>
    <div class="w-full px-2 py-4 rounded-lg shadow-lg mb-7">
      <h3 class="mb-2 text-2xl font-bold text-red-500">接続方法</h3>
      <h3 class="mb-2 text-2xl font-bold">
        <% @keyboard.connect.each do |connect| %>
          <%= connect %>
        <% end %>
      </h3>
    </div>

    <div class="flex justify-between">
    <div class="w-full px-2 py-4 rounded-lg shadow-lg mb-7">
      <h3 class="mb-2 text-2xl font-bold"><%= @keyboard.size %></h3>
    </div>
      <div class="w-full px-2 py-4 rounded-lg shadow-lg mb-7">
        <h3 class="mb-2 text-2xl font-bold"><%= @keyboard.layout %></h3>
      </div>
      <div class="w-full px-2 py-4 rounded-lg shadow-lg mb-7">
        <h3 class="mb-2 text-2xl font-bold"><%= @keyboard.switch %>スイッチ</h3>
      </div>
    </div>

    <div class="px-2 py-4 rounded-lg shadow-lg mb-7">
      <h3 class="mb-2 text-2xl font-bold text-red-500">詳細情報</h3>
      <div class="mb-2 text-lg">
        <%= @keyboard.caption %>
      </div>
    </div>

  </div>

</div>
<br>
