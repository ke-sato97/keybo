<%= form_with model: comment, url: [keyboard, comment], local: true do |f| %>
  <%= render 'shared/flash_message' %>
  <br>
  <%= f.label :body %><br>
  <%= f.text_area :body %>
  <br>
  <%= f.submit "送信", class:'mt-1 mb-1 bg-cyan-500 text-white hover:bg-cyan-600 px-2 py-1' %>
<% end %>

