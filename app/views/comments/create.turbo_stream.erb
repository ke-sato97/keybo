<%# turbo_stream.アクション名, ターゲットのid %>
<%= turbo_stream.append, @comment %>

<%= turbo_stream.update "new_comment", partial: 'comments/form', locals: { keyboard: @keyboard, comment: Comment.new } %>

<%= turbo_stream.update "flash", partial: "shared/flash_message" %>
