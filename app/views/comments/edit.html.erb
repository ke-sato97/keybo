<%= turbo_frame_tag dom_id(@comment) do %>
  <%= render 'comments/form', commentable: @keybaord, comment:
  <%= link_to t('defaults.cancel'), @keybaord, class: 'btn btn-secondary btn-sm float-end mt-2' %>
<% end %>
